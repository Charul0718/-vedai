[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "knowledge-engine"
version = "1.0.0"
description = "Structured astrology knowledge base for VedAI"
requires-python = ">=3.9"
dependencies = []
